# Used by Hardhat and contracts/scripts/deploy.ts
# Copy to `.env` in this directory or export before running deploy commands.

# Required for deployments and scripted txs
ETHEREUM_PRIVATE_KEY=

# RPC for the network you deploy to (default in hardhat.config is Base mainnet URL if unset)
ETHEREUM_RPC_URL=https://mainnet.base.org
